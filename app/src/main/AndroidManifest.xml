<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.homeworkapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HomeworkApplication">
        <activity android:name=".AboutClassActivity"></activity>
        <activity android:name=".AboutStudentActivity" />
        <activity android:name=".AboutLessonActivity" />
        <activity android:name=".AboutHomeworkActivity" />
        <activity android:name=".AboutAppActivity" />
        <activity android:name=".ClassInsertActivity" />
        <activity android:name=".ClassActivity" />
        <activity android:name=".LessonInsertActivity" />
        <activity android:name=".LessonActivity" />
        <activity android:name=".StudentInsertActivity" />
        <activity android:name=".StudentListViewActivity" />
        <activity android:name=".StudentActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>